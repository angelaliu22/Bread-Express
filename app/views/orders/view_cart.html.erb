#be able to change quantity

<div class="row">
  <div class="small-7 columns">
      <% if !@cart_order_items.empty? %>
        <div class="panel clearfix">
            <h3>Your cart </h3>
            <ul>
                <% @cart_order_items.each do |order_item| %>
                    <% unless order_item.item.picture_url.nil? %>
                        <p><%= image_tag order_item.item.picture_url %></p> 
                    <% end %>
                    <li><%= link_to order_item.item.name, item_path(order_item.item) %></li>
                    <li><p>Quantity: </p> <%= order_item.quantity %></li>
                <li><p>Subtotal: </p> <%= order_item.subtotal %></li>
                <li><%= link_to "Delete Item", remove_item_path(order_item.item.id) %></li>
              <% end %>
            </ul>

          </div>
            <%= link_to "Checkout", checkout_cart_path %>
      <% else %>
      <h3>There are no items in your cart</h3>
    <% end %>
  </div>
</div>